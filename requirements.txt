# Dependensi LANGSUNG saja — pip menarik transitif minimal otomatis.
# Dibuat ramping agar bundle serverless Vercel < 225 MB.
# Jangan tambah paket tak terpakai (mis. SDK Google legacy) — cek impor di core/services.py dulu.
Django==5.2.17
dj-database-url==3.1.2
django-jazzmin==3.0.5
google-genai==2.17.0
gunicorn==26.0.0
pydantic==2.13.4
PyMySQL==1.1.1
pypdf==6.15.0
python-dotenv==1.2.2
whitenoise==6.12.0
youtube-transcript-api==1.2.4
